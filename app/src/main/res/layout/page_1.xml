<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="homeScreenViewModel"
            type="com.example.myapplication.viewmodel.HomeViewModel" />

        <import type="android.view.View" />
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="@{homeScreenViewModel.progressLiveData ? View.GONE : View.VISIBLE}" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{homeScreenViewModel.progressLiveData ? View.VISIBLE : View.GONE}" />
    </FrameLayout>
</layout>